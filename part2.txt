CREATE TABLE A (
   a1 INTEGER NOT NULL,
   a2 INTEGER,
   a3 INTEGER,
   PRIMARY KEY (a1)
);

CREATE TABLE B (
   b1 INTEGER NOT NULL,
   PRIMARY KEY (b1),
   FOREIGN KEY (b1) REFERENCES A(a1)
);

CREATE TABLE C (
   c1 INTEGER NOT NULL,
   PRIMARY KEY (c1),
   FOREIGN KEY (c1) REFERENCES A(a1)
);

CREATE TABLE Z (
   z1 INTEGER NOT NULL,
   PRIMARY KEY (z1)
);

CREATE TABLE Y (
   y1 INTEGER NOT NULL,
   PRIMARY KEY (y1)
);

CREATE TABLE D (
   d1 INTEGER NOT NULL,
   d2 INTEGER,
   d3 INTEGER,
   PRIMARY KEY (d1)
);

CREATE TABLE X (
   x1 INTEGER NOT NULL,
   PRIMARY KEY (x1)
);

CREATE TABLE W (
   w1 INTEGER NOT NULL,
   PRIMARY KEY (w1)
);

CREATE TABLE E (
   e1 INTEGER NOT NULL,
   e2 INTEGER,
   PRIMARY KEY (e1)
);

CREATE TABLE F (
   f1 INTEGER NOT NULL,
   f2 INTEGER,
   PRIMARY KEY (f1)
);

CREATE TABLE U (
   u1 INTEGER NOT NULL,
   PRIMARY KEY (u1)
);

CREATE TABLE G (
   g1 INTEGER NOT NULL,
   PRIMARY KEY (g1)
);

CREATE TABLE H (
   h1 INTEGER NOT NULL,
   PRIMARY KEY (h1)
);

CREATE TABLE T (
   t1 INTEGER NOT NULL,
   PRIMARY KEY (t1)
);

