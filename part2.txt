CREATE TABLE A (
   a1 INTEGER NOT NULL,
   a2 INTEGER,
   a3 INTEGER,
   PRIMARY KEY (a1)
);

CREATE TABLE B (
   b1 INTEGER NOT NULL,
   PRIMARY KEY (b1),
   FOREIGN KEY (b1) REFERENCES A(a1)
);

CREATE TABLE C (
   c1 INTEGER NOT NULL,
   PRIMARY KEY (c1),
   FOREIGN KEY (c1) REFERENCES A(a1)
);

CREATE TABLE Z (
   z1 INTEGER NOT NULL,
   PRIMARY KEY (z1)
);

CREATE TABLE Y (
   y1 INTEGER NOT NULL,
   PRIMARY KEY (y1)
);

CREATE TABLE D (
   d1 INTEGER NOT NULL,
   d2 INTEGER,
   d3 INTEGER,
   PRIMARY KEY (d1)
);

CREATE TABLE X (
   x1 INTEGER NOT NULL,
   PRIMARY KEY (x1)
);

CREATE TABLE W (
   w1 INTEGER NOT NULL,
   PRIMARY KEY (w1)
);

CREATE TABLE E (
   e1 INTEGER NOT NULL,
   e2 INTEGER,
   PRIMARY KEY (e1)
);

CREATE TABLE F (
   f1 INTEGER NOT NULL,
   f2 INTEGER,
   PRIMARY KEY (f1)
);

CREATE TABLE U (
   u1 INTEGER NOT NULL,
   PRIMARY KEY (u1)
);

CREATE TABLE G (
   g1 INTEGER NOT NULL,
   PRIMARY KEY (g1)
);

CREATE TABLE H (
   h1 INTEGER NOT NULL,
   PRIMARY KEY (h1)
);

CREATE TABLE T (
   t1 INTEGER NOT NULL,
   PRIMARY KEY (t1)
);

CREATE TABLE BZYD_Relation (
   b1 INTEGER NOT NULL,
   z1 INTEGER NOT NULL,
   y1 INTEGER NOT NULL,
   d1 INTEGER NOT NULL,
   PRIMARY KEY (b1, z1, y1, d1),
   FOREIGN KEY (b1) REFERENCES B(b1),
   FOREIGN KEY (z1) REFERENCES Z(z1),
   FOREIGN KEY (y1) REFERENCES Y(y1),
   FOREIGN KEY (d1) REFERENCES D(d1)
);


CREATE TABLE D_H_V (
   d1 INTEGER NOT NULL,
   h1 INTEGER NOT NULL,
   PRIMARY KEY (d1, h1),
   FOREIGN KEY (d1) REFERENCES D(d1),
   FOREIGN KEY (h1) REFERENCES H(h1)
);


CREATE TABLE D_X_W_Relation (
   d1 INTEGER NOT NULL,
   x1 INTEGER NOT NULL,
   w1 INTEGER NOT NULL,
   PRIMARY KEY (d1, x1, w1),
   FOREIGN KEY (d1) REFERENCES D(d1),
   FOREIGN KEY (x1) REFERENCES X(x1),
   FOREIGN KEY (w1) REFERENCES W(w1)
);


ALTER TABLE U
  ADD COLUMN e1 INTEGER NOT NULL,
  ADD COLUMN f1 INTEGER NOT NULL,
  ADD CONSTRAINT fk_u_e 
      FOREIGN KEY (e1) REFERENCES E(e1),
  ADD CONSTRAINT fk_u_f
      FOREIGN KEY (f1) REFERENCES F(f1);

CREATE TABLE G_T_Relation (
   g1 INTEGER NOT NULL,
   t1 INTEGER NOT NULL,
   PRIMARY KEY (g1, t1),
   FOREIGN KEY (g1) REFERENCES G(g1),
   FOREIGN KEY (t1) REFERENCES T(t1)
);

